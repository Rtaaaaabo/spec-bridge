# CLAUDE.md

このリポジトリ（spec-bridge / Askless）で作業する Claude への申し送り。
コードの不変条件は [CONTRIBUTING.ja.md](CONTRIBUTING.ja.md) にある。ここには、これまでに決まった方針と実測値を置く。
個人の作業環境に依存する指示は、各自の `CLAUDE.local.md`（Git の管理外）に書く。

## 決まっていること

- **LP の旗は「書いていないコードについて、根拠付きで話す」。** FDE・前任者からの引き継ぎ・技術 DD・自社プロダクトを同じ文言で試している。
  LP の見出しには「FDE」という語を使わない（Zenn などの記事では使ってよい）。
  waitlist で「使う場面」と流入元（`?ref=`）を取っている。
- **確認事項は3種類**（`intent` 聞くべきこと／`unverified` 追加で調べれば埋まる／`scope` 範囲のメモ）。
  `intent` は、エージェントが実際に開いたファイル（`searched`）がなければ `unverified` に格下げされる。
  詳細は CONTRIBUTING.ja.md の不変条件 2。
- **プロンプトの読み手は「このコードを書いていない人」。** CS/QA に固定しない。コードから分からない事実（問い合わせの多さ・利用状況・予定）を書かせない。
- **デモページ（askless-landing の `specs/`）は生成物に手を加えずに載せる。**

## 原価の目安（gitea で実測、API 料金換算）

| 用途 | 目安 |
| --- | --- |
| 既存コードからの書き起こし（backfill） | 1機能 約 $1.7・約4分半（3回とも同水準） |
| PR 追従・仕様に影響する小〜中規模の PR | 1件 約 $1・約3分 |
| PR 追従・仕様に影響しない PR | 1件 約 $0.03〜0.23・数秒 |
| PR 追従・大規模 PR（70ファイル級） | 今は約 $1.3 だが読み切れていない（変更ファイル読了 0.11）。きちんと読めば数倍 |
